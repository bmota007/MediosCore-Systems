[from-telnyx]
; Medios Corporativos (MC)
exten => 12815490674,1,NoOp(=== MC CALL RECOVERED ===)
 same => n,Answer()
 same => n,Playback(custom/mc-main-greeting)
 same => n,Hangup()

; ProWork (PW)
exten => 15012741821,1,NoOp(=== PW CALL RECOVERED ===)
 same => n,Answer()
 same => n,Playback(custom/prowork-welcome)
 same => n,Hangup()

; Catch-all for other numbers
exten => _.,1,NoOp(=== CATCH-ALL: ${EXTEN} ===)
 same => n,Answer()
 same => n,Playback(demo-thanks)
 same => n,Hangup()

[default]
exten => _X.,1,Goto(from-telnyx,${EXTEN},1)
